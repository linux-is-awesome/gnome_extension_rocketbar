import { gettext } from 'resource:///org/gnome/shell/extensions/extension.js';
import SharedLabels from '../../shared/core/labels.js';

/** @enum {string} */
export const Labels = SharedLabels(gettext);
